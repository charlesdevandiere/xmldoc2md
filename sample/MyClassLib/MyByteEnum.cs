namespace MyClassLib;

/// <summary>
/// An enum with a <see cref="byte"/> underlying type.
/// </summary>
public enum MyByteEnum : byte
{
    /// <summary>The zero value.</summary>
    Zero = 0,
    /// <summary>The one value.</summary>
    One = 1,
    /// <summary>The two value.</summary>
    Two = 2,
}
